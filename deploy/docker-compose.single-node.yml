version: "3.9"

services:
  qai-node:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ..:/app
    command: bash -lc "pip install -r requirements.txt && python scripts/run_node.py"
    ports:
      - "8000:8000"
